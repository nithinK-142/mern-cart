{
  "version": 2,
  "builds": [
    {
      "src": "./build/index.js",
      "use": "@vercel/node"
    }
  ],
  "env": {
    "BRANCH": "vercel"
  },
  "scripts": {
    "vercel-deploy": "if [ \"$BRANCH\" != \"main\" ]; then echo \"Cannot deploy from $BRANCH branch\"; exit 1; fi"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
